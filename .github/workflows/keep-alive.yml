name: Keep Supabase Alive

on:
  schedule:
    - cron: "0 12 */4 * *"   # every 4 days at 12:00 UTC
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping keep alive endpoint
        run: curl -fsS https://trading-journal-lime-three.vercel.app/api/keep-alive
